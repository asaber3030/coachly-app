import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({ button: { alignItems: 'center', backgroundColor: '#1d4ed8', borderRadius: 8, padding: 14 }, disabled: { opacity: 0.5 }, label: { color: '#fff', fontWeight: '700' } });